Create a monorepo using Turborepo. The project should contain the following things:

- TypeScript library to be used by web applications (should be designed so it can be consumed by other applications). Details are can be found in `docs/js-lib/goal.md`
- A React Vite web application. Details can be found in `docs/web-app/goal.md`
